
/// help types for apply effect on [Builder](hyper::server::Builder)
pub mod service;
/// help type for apply effect on graceful shutdown
pub mod graceful;
/// help type for apply effect on adding extension
pub mod extension;
/// help types for apply effect on [Router](axum::Router)
pub mod router;
